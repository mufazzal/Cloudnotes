shell commands ->
  ls
  cat
See how if we chose diffrent geographical location, the latancy impact by
  tracert <IP>  command
  
  
AWS :-
  AWS SSO
  X-ray
  SQS  SNS   Kenesis
  
  
Concepts:-  
  CIDR (Classless Inter Domain Routing )
  IPV4 vs IPV6
  reserved IP addresses

Storage gateway
ALB routing by header and ip
